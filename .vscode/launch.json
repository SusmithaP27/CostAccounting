{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Cost accounting web",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build",
            "program": "${workspaceFolder}/bin/Debug/net10.0/CostAccounting.dll",
            "args": [],
            "cwd": "${workspaceFolder}",
            "serverReadyAction":{
                "action": "openExternally",
                "pattern": "listening on port ([0-9]+)",

            }
        }
    ]
}